<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="maintenance"
            type="com.example.opulexpropertymanagement.models.Maintenance" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:orientation="vertical">

        <TextView
            app:layout_constraintStart_toStartOf="parent"
            android:id="@+id/textview_maintenance_title"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="    Maintenance"
            android:textColor="@color/colorVsLight"
            android:textSize="18sp"/>
        <TextView
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textview_maintenance_title"
            android:id="@+id/textview_maintenance_description"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@{maintenance.description}"
            android:textColor="@color/colorVsLight"
            android:textSize="16sp"
            tools:text="DESCRIPTION" />

        <ImageView
            android:id="@+id/btn_trash"
            android:tint="@color/colorAccent"
            android:layout_marginTop="3dp"
            android:padding="4dp"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_basic_trash"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>